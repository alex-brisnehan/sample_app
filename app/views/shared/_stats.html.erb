<% @user ||= current_user %>
<div class="stats">
  <table summary="User stats">
    <tr>
      <td>
        <a href="<%= followers_user_path(@user) %>">
          <span id="following" class="stat">
            <%= @user.following.count + @user.followers.count%> friends
          </span>
        </a>
      </td>
    </tr>
  </table>
</div>
